{
  "name": "@enistere/foundation-kernel-extensions",
  "version": "0.1.0",
  "private": true,
  "description": "Enistere Foundation — E2 Adapter Protocol v0: adapter manifest schema, RuntimeAdapter contract, artifact planning and ownership rules. No I/O, no framework.",
  "license": "UNLICENSED",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./schemas/*": "./schemas/*"
  },
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@enistere/foundation-kernel-compiler": "0.1.0",
    "@enistere/foundation-kernel-contracts": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
